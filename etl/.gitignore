.venv
__pycache__
*.pyc
.pytest_cache
.ruff_cache
*.egg-info

# The root .gitignore ignores any `data/` directory (for the app's SQLite
# file); re-include the partner clinic feeds this pipeline ships and tracks.
!data/
!data/feeds/
